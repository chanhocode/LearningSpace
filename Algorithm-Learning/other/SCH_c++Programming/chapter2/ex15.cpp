// µ¡¼À, »¬¼À, °ö¼À, ³ª´°¼À, ³ª¸ÓÁö ¿¬»ê
//#include <iostream>
//using namespace std;
//
//int main() {
//    int a, b;
//    char mode;
//    while (true) {
//        cout << "? ";
//        cin >> a >> mode >> b;
//        if (cin.eof() == true) break;
//        if (mode == '+') cout << a << ' ' << mode << ' ' << b << " = " << a + b << endl;
//        else if (mode == '-') cout << a << ' ' << mode << ' ' << b << " = " << a - b << endl;
//        else if (mode == '*') cout << a << ' ' << mode << ' ' << b << " = " << a * b << endl;
//        else if (mode == '/') cout << a << ' ' << mode << ' ' << b << " = " << a / b << endl;
//        else if (mode == '%') cout << a << ' ' << mode << ' ' << b << " = " << a % b << endl;
//    }
//}

/*

int main() {
	char op;
	int a, b, res;

	while (true)
	{
		cout << "? ";
		cin >> a >> op >> b;

		switch (op)
		{
		case '+':
			res = a + b;
			break;
		case '-':
			res = a - b;
			break;
		case '*':
			res = a * b;
			break;
		case '%':
			res = a % b;
			break;
		case '/':
			res = a / b;
			break;
		default:
			break;
		}

		cout << a << " " << op << " " << b << " = " << res << '\n';
	}

	return 0;
}
*/