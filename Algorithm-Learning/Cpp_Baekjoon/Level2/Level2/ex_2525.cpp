/*
#include <iostream>
using namespace std;

int main() {
    int a, b, c, d, e;
    cin >> a >> b >> c;

    if (b + c >= 60) {
        d = (b + c) / 60;
        e = (b + c) % 60;
        if (a + d > 23) {
            a = (a + d) - 24;
        }
        else {
            a = a + d;
        }
        b = e;
    }
    else {
        b = b + c;
    }
    cout << a << ' ' << b;
}
*/